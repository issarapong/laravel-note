1 Browse to Root folder 
 composer global require laravel/installer
 composer create-project --prefer-dist laravel/laravel lara-app "6.*"

2 Open VS code @ lara-app folder

git init 
git add .
git commit -m  “larval 6 default”

git remote add origin https://github.com/issarapong/lara-app.git
git branch -M main
git push -u origin main


Verify remote git

if you see after git remote add origin https://github.com/issarapong/lara-app.git

 “fatal: remote cheese already exists.”
 git remote remove origin
